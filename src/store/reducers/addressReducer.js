import {UPDATE_ADDRESS} from '../actions/addressActions'

const initialState = {
    address: {}
}

export default (state=initialState, action)=>{
    switch(action.type){
        case UPDATE_ADDRESS: 
            const 
    }
}